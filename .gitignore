# Godot 4+ specific ignores
.godot/

# Legacy Godot import cache
.import/

# Exported builds
/build/
/export/

# Export configs (may contain keystore paths / passwords)
export.cfg
export_presets.cfg

# Imported translations (auto-generated from CSV)
*.translation

# Mono / .NET (unused — GDScript project — but harmless to guard)
.mono/
data_*/
mono_crash.*.json

# OS / editor cruft
.DS_Store
Thumbs.db
.directory
*~
